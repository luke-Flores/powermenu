[package]
name = "powermenu"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[profile.release]
opt-level = 3
lto = true
codegen-units =1
strip = true

[dependencies]
ncurses = "5.101.0"

